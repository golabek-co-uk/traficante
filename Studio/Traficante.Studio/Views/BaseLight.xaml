<Styles xmlns="https://github.com/avaloniaui"
        x:Class="Traficante.Studio.Views.BaseLight"
        xmlns:AvalonEdit="clr-namespace:AvaloniaEdit;assembly=AvaloniaEdit"
        xmlns:idc="clr-namespace:Dock.Avalonia.Controls;assembly=Dock.Avalonia"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d">

  <Style Selector="Button">
    <Setter Property="Padding" Value="3 2 3 3" />
  </Style>
  
  <Style Selector="Button.Secondary">
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="BorderBrush" Value="Transparent"/>
  </Style>
  

  <Style Selector="Button.Secondary:pointerover">
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="Background" Value="{DynamicResource ThemeAccentBrush4}"/>
    <Setter Property="BorderBrush" Value="{DynamicResource ThemeAccentBrush}"/>
  </Style>

  <Style Selector="Button.Secondary:pressed /template/ ContentPresenter">
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="Background" Value="{DynamicResource ThemeAccentBrush4}"/>
    <Setter Property="BorderBrush" Value="{DynamicResource ThemeAccentBrush}"/>
  </Style>

  <Style Selector="Border.Selectabled">
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="BorderBrush" Value="Transparent"/>
    <Setter Property="Background" Value="Transparent" />
  </Style>

  <Style Selector="Border.Selectabled:pointerover">
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="BorderBrush" Value="{DynamicResource ThemeAccentBrush}"/>
    <Setter Property="Background" Value="{DynamicResource ThemeAccentBrush4}"/>
  </Style>

  <Style Selector="Border.Selectabled:pressed /template/ ContentPresenter">
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="BorderBrush" Value="{DynamicResource ThemeAccentBrush}"/>
    <Setter Property="Background" Value="{DynamicResource ThemeAccentBrush4}"/>
  </Style>

  <Style Selector="TextBox#SearchBox">
    <Setter Property="BorderBrush" Value="Transparent"/>
    <Setter Property="BorderThickness" Value="0"/>
  </Style>

  <Style Selector="Border.SearchBox">
    <Setter Property="Background" Value="{DynamicResource ThemeBackgroundBrush}"/>
    <Setter Property="BorderBrush" Value="{DynamicResource ThemeBorderMidBrush}"/>
    <Setter Property="BorderThickness" Value="{DynamicResource ThemeBorderThickness}"/>
  </Style>
  
  <Style Selector="DataGrid">
    <Style.Resources>
    </Style.Resources>
    <Setter Property="RowBackground" Value="#FFFFFFFF" />
    <Setter Property="AlternatingRowBackground" Value="#FFFFFFFF" />
    <Setter Property="GridLinesVisibility" Value="All" />
    <Setter Property="HorizontalGridLinesBrush" Value="{DynamicResource ThemeControlMidBrush}" />
    <Setter Property="VerticalGridLinesBrush" Value="{DynamicResource ThemeControlMidBrush}" />
    <Setter Property="Background" Value="#FFFFFFFF" />
    <Setter Property="Padding" Value="0" />
    <Setter Property="Margin" Value="0" />
    <!--<Setter Property="BorderBrush" Value="{DynamicResource ThemeControlMidHighBrush}"/>
    <Setter Property="BorderThickness" Value="1" />-->
  </Style>
  
  <Style Selector="DataGridColumnHeader">
    <Setter Property="Background" Value="#FFFFFFFF"/>
    <Setter Property="AreSeparatorsVisible" Value="false" />
    <Setter Property="AreSeparatorsVisible" Value="true" />

  </Style>

  <Style Selector="DataGridColumnHeader /template/ Rectangle#VerticalSeparator">
    <Setter Property="Stroke" Value="#FFF5F5F5"/>
    <Setter Property="StrokeThickness" Value="1"/>
  </Style>
  
  <Style Selector="DataGridColumnHeader /template/ Rectangle#HorizontalSeparator">
    <Setter Property="Stroke" Value="Black"/>
    <Setter Property="StrokeThickness" Value="1"/>
  </Style>
  
  <Style Selector="TabControl">
      <Setter Property="Background" Value="Transparent"/>
      <Setter Property="Foreground" Value="{DynamicResource ThemeForegroundBrush}"/>
  </Style>
  <Style Selector="TabControl WrapPanel">
      <Setter Property="Background" Value="Transparent"/>
  </Style>

  <Style Selector="TabItem">
      <Setter Property="FontSize" Value="12"/>
      <Setter Property="Height" Value="24"/>
      <Setter Property="VerticalAlignment" Value="Bottom"/>
      <Setter Property="Background" Value="Transparent"/>
      <Setter Property="Foreground" Value="{DynamicResource ThemeForegroundBrush}"/>
      <Setter Property="Padding" Value="3 3 3 3"/>
  </Style>
  <Style Selector="TabItem ControlTemplate ContentPresenter#PART_ContentPresenter">
    <Setter Property="BorderThickness" Value="0"/>
  </Style>
  <Style Selector="TabItem:pointerover /template/ ContentPresenter#PART_ContentPresenter">
      <Setter Property="Background" Value="Transparent"/>
  </Style>

  <Style Selector="TabItem:focus">
      <Setter Property="Foreground" Value="{DynamicResource ThemeForegroundColor}"/>
      <Setter Property="Padding" Value="3 3 3 3"/>
  </Style>
  <Style Selector="TabItem:focus /template/ ContentPresenter#PART_ContentPresenter">
      <Setter Property="Background" Value="Transparent"/>
  </Style>

  <Style Selector="TabItem:selected">
    <Setter Property="Padding" Value="3 3 3 3"/>
  </Style>
  <Style Selector="TabItem:selected /template/ ContentPresenter#PART_ContentPresenter">
    <Setter Property="Background" Value="Transparent"/>
    <Setter Property="BorderThickness" Value="0 0 0 2"/>
    <Setter Property="BorderBrush" Value="#1C97EA"/>
  </Style>

  <Style Selector="ScrollBar:vertical /template/ Grid">
    <Setter Property="Background" Value="White"/>
  </Style>
  <Style Selector="ScrollBar:vertical /template/ Track RepeatButton">
    <Setter Property="Background" Value="White"/>
  </Style>
  <Style Selector="ScrollBar:vertical /template/ RepeatButton#PART_LineUpButton">
    <Setter Property="Background" Value="White"/>
  </Style>
  <Style Selector="ScrollBar:vertical /template/ RepeatButton#PART_LineDownButton">
    <Setter Property="Background" Value="White"/>
  </Style>
  
  <Style Selector="ScrollBar:horizontal /template/ Grid">
    <Setter Property="Background" Value="White"/>
  </Style>
  <Style Selector="ScrollBar:horizontal /template/ Track RepeatButton">
    <Setter Property="Background" Value="White"/>
  </Style>
  <Style Selector="ScrollBar:horizontal /template/ RepeatButton#PART_LineUpButton">
    <Setter Property="Background" Value="White"/>
  </Style>
  <Style Selector="ScrollBar:horizontal /template/ RepeatButton#PART_LineDownButton">
    <Setter Property="Background" Value="White"/>
  </Style>
  <!--<Style Selector="ScrollViewer /template/ Grid">
    <Setter Property="Background" Value="White"/>
  </Style>-->
  <Style Selector="ScrollViewer /template/ Panel">
    <Setter Property="Background" Value="White"/>
    <Setter Property="Background" Value="Red"/>
  </Style>
  
  <Style Selector="DataGrid ScrollBar:horizontal /template/ Border">
    <Setter Property="BorderBrush" Value="{DynamicResource ThemeControlMidBrush}"/>
    <Setter Property="BorderThickness" Value="0 1 0 0"/>  
  </Style>
  
    <Style Selector="DataGrid ScrollBar:vertical /template/ Border">
    <Setter Property="BorderBrush" Value="{DynamicResource ThemeControlMidBrush}"/>
    <Setter Property="BorderThickness" Value="1 0 0 0"/>  
  </Style>
 
  <Style Selector="DockPanel#QueryViewHeader">
    <Setter Property="Background" Value="Transparent"/>
    <Setter Property="Margin" Value="4 5 4 5" />
  </Style>

  <Style Selector="idc|DockToolChrome /template/ Border#PART_Border">
    <Setter Property="BorderThickness" Value="1 1 1 1" />
  </Style>

  <Style Selector="idc|DockToolChrome:active /template/ Grid#PART_Grip">
    <Setter Property="Background" Value="{DynamicResource ThemeBackgroundBrush}" />
    <Setter Property="TextBlock.Foreground" Value="{DynamicResource ThemeForegroundBrush}" />
  </Style>

  <Style Selector="idc|DocumentControl">
    <Setter Property="Margin" Value="0 4 0 0" />
  </Style>

</Styles>