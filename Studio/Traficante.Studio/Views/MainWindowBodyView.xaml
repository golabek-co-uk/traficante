<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:views="clr-namespace:Traficante.Studio.Views;assembly=Traficante.Studio"
             xmlns:idc="clr-namespace:Dock.Avalonia.Controls;assembly=Dock.Avalonia"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="Traficante.Studio.Views.MainWindowBodyView">
  <Grid RowDefinitions="Auto,Auto,*" ColumnDefinitions="*" Background="Transparent" Focusable="True">
    <views:MainWindowMenuView Name="Menu" Grid.Row="0" Grid.Column="0"></views:MainWindowMenuView>
    <views:MainWindowToolBarView Name="ToolBar" Grid.Row="1" Grid.Column="0"></views:MainWindowToolBarView>
    <ContentControl Content="{Binding CurrentView}" Grid.Row="2" Grid.Column="0"/>
  </Grid>
</UserControl>
